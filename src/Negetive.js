import './Positive.css';

function Negetive(){

    // setTimeout(() => {
    //     const elem = document.getElementById("result");
    //     elem.parentNode.removeChild(elem);
    // }, 2000);

    return (
        <div>
            <h3 className="result" id="result" >Your Statememt represent <label className="negetive">Negetive</label> Sentiments</h3>
        </div>
    );
}

export default Negetive;